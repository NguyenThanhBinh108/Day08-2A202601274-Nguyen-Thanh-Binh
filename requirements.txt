# Core
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0

# Task 2 - Crawling
# Pin <0.9: crawl4ai 0.9.x doi unclecode-litellm -> openai>=2.8, xung dot voi ragas (openai<2).
# Pin litellm <1.93: rieng ban 1.93.0 chi co sdist, pip se doi Rust toolchain de build va fail.
# Sau khi cai xong phai chay them: playwright install chromium
crawl4ai>=0.3.0,<0.9
litellm>=1.53.1,<1.93

# Task 3 - Document conversion
markitdown[pdf]>=0.1.0
fpdf2>=2.7.0

# Task 4 - Chunking & Indexing
# Pin <0.3: ragas 0.1.21 yeu cau langchain-core<0.3, ban 1.x se xung dot
langchain-text-splitters>=0.2.0,<0.3
chromadb>=0.5.0

# Task 4/5 - Embedding provider (mac dinh: sentence_transformers, local, khong can API key)
# Doi provider qua bien EMBEDDING_PROVIDER trong .env: sentence_transformers | google | openai
sentence-transformers>=2.2.0
# google-generativeai>=0.5.0  # Chi can neu EMBEDDING_PROVIDER=google (khong can neu dung mac dinh)
# openai (provider "openai") da co san o muc Task 10 ben duoi, khong can cai them

# Task 6 - BM25 + TF-IDF
rank-bm25>=0.2.2
scikit-learn>=1.3.0

# Task 8 - PageIndex
pageindex>=0.1.0

# Task 10 - Generation
# Pin <2.0.0: ragas 0.1.21 -> langchain-openai 0.1.x chi chay voi openai 1.x
openai>=1.30.0,<2.0.0

# Group project - UI
streamlit>=1.35.0

# Group project - Evaluation
# ragas 0.1.21 pin langchain-core<0.3 -> phai ghim ca ho langchain va datasets ve dong cu,
# neu khong pip se backtrack rat sau va roi vao ban source cua litellm (doi Rust toolchain de build).
ragas==0.1.21
datasets>=2.14.0,<3
langchain-openai>=0.1.0,<0.2

# Testing (dung de cham diem bai ca nhan)
pytest>=7.0.0
